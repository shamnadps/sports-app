version: '3.3'

services:

  postgres-dev:
    image: postgres:10.3
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
      POSTGRES_DB: vanta_pwa

  admin:
    image: adminer
    ports:
      - 8080:8080